# MotiveRevival

A celestial-atlas music player for Windows. Browse your library as drifting constellations of album art, summon any song with a keystroke, and watch the interface breathe with the music.

---

## Vision

A local music frontend that feels **cosmic, aetherial, and fantastical** — built around three pillars:

- **Universal search, always on** — start typing anywhere; fuzzy, accent-blind matching across songs, artists, and albums. `Esc` cancels, `Enter` summons.
- **osu!-style browsing** — a momentum carousel of rounded art cards that breathe with scroll velocity; click an album to descend into its songs.
- **A living interface** — each album's dominant colors wash the stage; gradients and glows pulse with live bass/mid/treble energy; synced lyrics will complete the picture.

## Tech Stack

| Layer | Choice | Why |
|---|---|---|
| Shell | **Electron** | Native file access + Chromium audio decode (FLAC/MP3/OGG) in one runtime |
| Language | **TypeScript** (strict) | Compile-time guard against the ghost bugs of attempts #1–2 |
| Bundler | **esbuild** | Sub-second builds. No dev server, no HMR — every launch is a true cold start |
| UI | **Vanilla TS + DOM** | Zero framework magic; explicit singletons, bind-once listeners |
| Tags & art | **music-metadata** | Pure JS; no native modules |
| Fonts | Sora · IBM Plex Mono via Fontsource | Local woff2, fully offline |
| Storage | JSON index + settings in `%APPDATA%\MotiveRevival` | Instant boot for <5k-track libraries |

## Architecture Rules

These exist to permanently kill the failure modes of previous attempts:

1. **One immortal `<audio>` element.** `PlayerService` owns it from boot to quit; nothing in the app ever destroys it. Navigation is overlays sliding over playback — returning to the library cannot brick the player.
2. **`requestAnimationFrame` clock**, never `timeupdate`, drives timeline and lyric sync.
3. **Cold starts only.** `npm run build` then `npm start`. HMR-style staleness is structurally impossible.
4. **`media://` streaming protocol** with range-request support and path containment (music folder + art cache only).
5. **Event buses over teardown**: player events (`Bus`) and selection events (`appBus`) fan out to persistent views; views subscribe once at boot.

### Module map

```
src/
├── main/            Electron main process
│   ├── main.ts        window, IPC, scan orchestration
│   ├── media.ts       media:// protocol (range requests, root containment)
│   ├── library.ts     scanner: tags, art extraction, palette extraction, index cache
│   └── settings.ts    persisted settings
├── preload/         contextBridge — the entire IPC surface
├── shared/types.ts  contracts shared across processes
└── renderer/
    ├── core/          player, buses, search index, fuzzy matcher, palette, band reactivity
    └── ui/            carousel physics, browser orchestrator, detail stage, transport, overlay
```

## Running

```
npm install        # once
npm run build      # bundle main/preload/renderer → dist/
npm start          # build + launch (always a cold start)
npm run typecheck  # strict TS gate
npm test           # LRC parser suite (20 cases)
```

Optional: set `MOTIVE_MUSIC_DIR` to override the music folder for a session. Default archive: `D:\Music` (changeable in-app via the sliders icon → Settings).

Keyboard: type anywhere = search · `/` focuses search · `Esc` steps back (search → overlay → detail) · `Space` play/pause · `Ctrl+R` cold reload · `F12` devtools.

## Roadmap — Movements

| # | Name | Contents | Status |
|---|---|---|---|
| I | Skeleton | Frameless window, media pipeline, immortal player, torture tests | ✅ Signed off |
| II | Data layer | Tag/artist/album/duration indexing, embedded-art extraction + folder fallback, cached index, background rescan | ✅ Signed off |
| III | The Browser | Universal always-on search, albums/artists/songs/playlists tabs, sorts, osu-style carousel v1, detail slide-in | ✅ Signed off |
| IV | Chromatic | Two-pane detail stage, palette engine, live audio-band reactivity, visualizer v2, queue + next/prev | ✅ Core signed off; display-toggles deferred |
| V | Voice | LRC parser (test-first), synced lyrics in stage rail + expanded panel, lrclib.net fetch writing `.lrc` beside files, lyric-focus/art-focus cycling | ✅ Signed off |
| VI | Pulse | Hover snippet previews w/ waveform-peak pipeline, song-reactive UI theming, velocity spacing + art parallax, card→fullscreen morph, search oracle, remembered now-playing view | ✅ Signed off (minor polish riders) |
| VII | Playlists | Create/reorder/persist; playlist tab awakens | ✅ Signed off |
| VIII | The Living Void | Pointer bias, song-state effects, The Waveform Horizon (Particle Lantern cursor + playing-row arrow markers live) | In progress — Phases 0, 3 v2, 5 closed; 4, 7 open; 6 partial |
| IX | The Crystal Atlas | 3D browse: Artist discography charts (albums absorbed), Song crystal plane (flat list + arrows retire), Constellation playlists; merged top bezel | Phase 1 bezel signed off; Phase 2 spike passed — the crystal river is the live Songs view (tested superior to the previous UI); Phase 3 parity next |
| X | The Universe | Rechartered as **The Library as Worlds**: the shelf (artist squares → discography → album place) with playlists one layer shorter, summon-only bezel; the night-sky direction retired | **Sole active focus.** Rechartered 2026-09-13 (`docs/charter-movement-x-v2.md`); Phase 0 sky disposition done; **Phase 1 — the Artists field — signed off 2026-09-14**; Phase 2 — the Album place — next |

## Progress Ledger

Sign-off entries are added when changes are verified working in session.

- **2026-08-26 — Movements I–III verified.** Playback pipeline, scrub-drag fix, transport wiring, numbering fix, borderless chrome, settings persistence, universal search relevance rewrite (word-boundary fuzzy), artist-card click fix (pointer-capture regression).
- **2026-08-26 — Movement IV verified.** Two-pane stage with staggered mini-cells; per-album queue context; auto-advance; prev/next; palette field throbbing confirmed; spectrum engine v2 confirmed "truly reflective" after fftSize 2048 rebuild; overlay color-wash pulse confirmed.
- **2026-08-26 — Movement V signed off.** Pure LRC parser locked behind a 20-case suite (`npm test`) covering fractions, multi-timestamps, offset sign convention, BOM/CRLF, malformed tolerance and sync boundaries. Lyrics resolve local-sheet-first, then lrclib (get → search fallback, closest duration, synced preferred) and are written beside songs so folders stay portable. LyricScroller lives in the stage rail *and* a two-column expanded panel: hue-tinted high-luminance ink, active-line glow, click-a-line-to-seek.
- **2026-08-26 — Movement VI signed off (minor polish riders).** Hover-snippet previews on every song cell: 1.4s dwell, main playback ducks to silence and restores precisely; waveform peaks come from a lazy-first-hover decode plus a throttled idle queue, persisted permanently. Song-reactive UI theming via `@property` color crossfade (priority hovered > playing > moonlight) across scrubber, glows, borders, ambient washes. Velocity spacing + art parallax on the carousel (transform-only). Card-to-fullscreen FLIP morph with measured destination rects. Search rebuilt as a top-center summonable oracle with centered browse chrome. Frameless custom window controls; flex-column shell; scroll-performance pass on long song lists. Now-playing focus modes gated on lyric availability and remembered between sessions (default: Art Focus).
- **2026-08-26 — Movement VII playlist implementation reached interaction-QA stage.** Renderer-owned playlist persistence through KV storage, live track resolution by ID/path, missing-track ghosts, create/rename/delete, drag or button reorder, session-only shuffle playback, play-all, playlist search, and song context-menu additions are present. Context menus now retain their anchor when changing views, use consistent typography, and prefer opening to the right of the source song. Default and expanded queue controls share reactive accent styling and explicit expanded-view hit-target handling. Full manual Electron QA and final sign-off remain pending.
- **2026-08-27 — Transport and now-playing polish verified.** Normal view is now the default playback surface; expanded now-playing is opened and closed with the music-note toggle or Escape. Both views share the minimized transport geometry and controls. Synced lyrics can preview over the normal scrubber with pointer-transparent frosted styling and an enlarged hit area; scrub release updates the active LRC line through the existing rAF clock.
- **2026-08-27 — Transport alignment and queue theming polished.** Expanded now-playing no longer shifts during transition; both transports place previous/play/next before the current-time display with matching geometry. The lyric backdrop is continuous and borderless, and queue panel accents now consume the active song theme variables.
- **2026-08-27 — Minimized lyric and settings polish verified.** Synced lyrics now appear as a centered content-sized orb above the minimized scrubber without a full-timeline frosted layer. Settings rows have more breathing room and their toggles/segments align to a shared right edge.
- **2026-08-27 — Scrubber atmosphere refined.** The minimized lyric orb now sits higher with larger type, and the scrubber rail, fill, knob, and hover glow carry a restrained active-song accent tint.
- **2026-08-27 — Playback presentation refined.** The lyric orb is now centered across the full transport and hidden on detail layers; the music-note halo is removed, no-lyrics expanded mode defaults to large art/visualizer focus with a restrained unavailable-action shake, and the queue remains palette-reactive.
- **2026-08-27 — Bottom-bar "horizon haze" signed off.** The transport bar retired its hairline border and now exhales an atmosphere: a viewport-scaled veil (`clamp(112px, 16vh, 172px)`, floored so lyrics never lose backing) rises above the controls — near-solid at the skyline (~97%), holding a 70%-density shoulder through its middle, dissolving into void at the crest. Three new registered colors `--hz-a/b/glow` derive dark-but-saturated tones from palette data and ride the 700ms crossfade chain; after live testing they follow the full hovered > playing priority chain beside every other accent (playing-only coupling read as disconnected). The minimized lyric preview shed its chip chrome — borderless ink resting inside the fog, viewport-centered at last (its old transport-relative anchor sat ~48px right of true center because of the asymmetric button cluster). Veil and lyric hush away together when detail or playlist layers open; expanded now-playing is untouched. Lessons banked for Movement VIII's rising dust: perceptibility lives in luminance deltas over the void backdrop, not opacity percentages shuffled among near-equal darks — one invisible-build regression was caught by eye and re-derived from first principles, and a later song-tinted mid-band experiment was judged less clean than the plain fade and reverted without residue.
- **2026-08-27 — Movement VII signed off.** Playlists completed final QA through days of live use: create/name/rename/delete, KV persistence, oracle rows, playlist cards, add-to-playlist context menus on songs and stage cells, and ghost rows for missing files. Reordering adopted the queue's grammar — gap-based drag with a glowing insertion line, the ▲▼ buttons retired, per-row ✕ retained, one drop one persist via `playlistsStore.reorderTrack`. Tab, search, play-all, and session-only shuffle all verified.
- **2026-08-27 — Expanded now-playing rebuilt around three first-class views.** Art / Art + Lyrics / Lyrics Only, switched by dots above the transport, keys 1/2/3, or the existing art-title cycling (legacy 'blur' preferences migrate to Lyrics Only). Lyrics Only keeps the blurred artwork as a full backdrop, hides the art column/visualizer/title, drops the divider, centers a wide lyric column, and keeps invisible edge zones for one-click view jumps. Lyrics render far larger (34px, 38px in Lyrics Only, × user scale) with looser leading (~5–6 lines on screen), and wheel-parking pauses auto-sync while scrolling with a 2.5s snap-home; clicking a line seeks and snaps instantly.
- **2026-08-27 — Performance campaign: virtualization, thumbnail pipeline, compositing.** The songs list virtualizes with fixed-height rows and patch-based sliding windows — visible rows are never remounted, relocation fires only after the offscreen buffer drains, and maintenance runs deferred — so scroll cost is independent of library size. Artwork became a two-size pipeline: 128px thumbnails generated at scan and self-healed on demand by the media handler, thumb-first rendering with full-res fallback, and cacheable art responses replacing the old no-store header that caused decode storms. Song rows hold permanent compositor surfaces, motion values are quantized, hover accent storms mute mid-fling, and the carousel paint loop touches only its visible window (including the discovery that the parallax transforms had been silently acting as our layer-promotion system). A blurred, crossfading backdrop of the playing album now spans the browsing floor. Transport numerals moved to high-luminance ink with a void halo; the album detail's back arrow retired in favor of Esc/click-outside; the topbar and controlbar joined the color-reactive system with a whisper-tier ceiling wash. Boot render-delay fell from ~1.13s to noise; scrolling is fluent at any velocity.
- **2026-08-27 — Movement VIII chartered: "The Living Void."** Decisions locked (partially amended same day — see next entry): the cursor becomes a lantern, and nearby ambient particles brighten and gently part around its light; music modulates the sky through *musical moments* (onset flux, never bass loudness) within a floor and a ceiling; each playing album draws a signature constellation — hash-seeded figure, palette color, duration-scaled sprawl — prerendered offscreen; the lantern cursor replaces the native pointer with an accent-rimmed core, wake, interactive ring, edge-chevron reveal, and a native I-beam yield on inputs; waveform-derived figures and similarity-clustered constellations are the north star beyond VIII; the folder-picker defers to Movement IX.
- **2026-08-27 — Ember particles deferred; browsing-floor backdrop experiments concluded.** After repeated roadblocks the canvas ember system was removed (`player.spectrum()` remains for a future pass). The floor then cycled through two replacements — a blurred-artwork backdrop, then a palette color wash masked so the horizon haze kept the floor — before both were reverted by design: the browsing floor is deliberately plain void, keeping the content surface calm. The archaic CSS starfield far plane (the source of the "random dots" on the timeline and elsewhere) was removed entirely. Meanwhile the "inaccurate UI colors" report was traced to hue derivation from near-neutral palette tones — a near-black dominant carries a mathematically arbitrary hue (landing on red) that the derivation's saturation floor then amplifies — and was **fixed** via chroma-weighted tone selection (`mostChromatic` in `palette.ts`) with a moonlight fallback for fully neutral palettes. No rescan was needed: stored palettes were always correct.
- **2026-08-27 — Movement VIII plan finalized.** Phase order amended by design discussion — Phase 3 lantern cursor, Phase 4 pointer bias (parallax), Phase 5 usability block, Phase 6 song-state effects (NEW: a solid corner-reticle framing the playing/selected song, and a dotted bracket plus a one-shot ripple on hovered rows — **solid means committed, dotted means preview**), Phase 7 **The Waveform Horizon** (NEW: a full-width waveform band across the timeline top, built from the Movement VI peaks pipeline, horizon-tinted and dim, with lyrics rendered over it and particles thrown from the playhead peaks on musical onsets; the haze gradient remains as the fallback until peaks are analysed, and a seek-on-waveform scrubber upgrade is the natural later step). Constellations move to the north star together with a possible library UI overhaul — including exploring a spatial, non-scroll alternative browsing view as an option, never a forced replacement.
- **2026-08-29 — Hover previews revised: pause-with-resume.** The preview dwell now fully pauses the main track (850ms fade-out, then a real pause — the transport shows it) and resumes at the exact position with a 950ms fade-in on hover end; leaving before the fade completes cancels the pause entirely. Hovering the currently-playing song is an audio no-op (theme push only — no more jump-to-offset echo), and a failed preview load restores the main track instead of stranding it silent. This revises Movement VI's duck-to-silence design after live-use rejection: volume-to-zero was indistinguishable from a pause that only lifted on hover exit, so the pause was made real and honest.
- **2026-08-29 — Songs entrance fix (row shrink).** Rows "turned a little shorter" ~1s after swapping to the Songs tab: the `song-in` entrance animation (`fill-mode: both`) overrode the cascade `transform` while the carousel kept writing `--cs` (center-compress up to 5% off-center); when the fill released at 900ms, rows snapped to the smaller cascade scale. Keyframes now compose the same scale — `scale(calc(var(--cs,1)*var(--hs,1))) translateY(...)` — so entrance and cascade agree and nothing snaps. Lesson banked: fill-mode animations must compose the transforms they temporarily replace.
- **2026-08-29 — Phase 3 lantern cursor: built, then deferred.** The custom cursor shipped end-to-end and iterated live: accent-tinted cursor images per state (default/interactive/drag/edge-left/right/settle) with hotspot-anchored PNG bitmaps, velocity wake with palette-memory motes, click burst, stop flare, ember-drip settle with orbiters, edge chevrons, native I-beam yield, and a `motionLantern` flag. Pulled cleanly after live-use blockers that proved platform-level: Electron drag regions (`-webkit-app-region: drag`) suppress the cursor to nothing (no arrow fallback), and Chromium suppresses cursor sprites whose bitmap extends past the window's client bounds — which condemns the top and bottom bars at any respectable sprite size. The interim manual window-dragging IPC (renderer pointer-capture → main-process cursor-poll reposition, release-at-top maximize) was also reverted: with a native cursor, the native drag region is strictly better (smoothness + full Aero Snap). Banked lessons: PNG beats SVG for cursor bitmaps; `cursor: image-set()` is unsupported and invalidates the declaration; unquoted `url()` cannot carry data-URI commas/semicolons; accent-tinted cursors must re-read the *applied* theme (push/pop chain), not the base. The lantern returns someday as a shelved rider — view-zone morphing included — with the platform law written down.
- **2026-08-29 — Phase 5 — Usability signed off.** First-run guidance and empty states: an empty or unreachable archive renders a centered guidance panel (title, one-line direction, Open Settings shortcut) instead of blank void, verified on the new `npm run start:fresh` sandbox harness (throwaway profile + scratch music folder — true day-one boot with zero risk to the real install). Scan clarity: the status line reports folders unreachable with an explanatory tooltip. Playlist duplicate protection: blocked in the store and marked "added" (disabled) in the context menu. Accent focus-visible rings for keyboard navigation, colored by the active song's chain. BPM tag: index format v5 reads `common.bpm`; the expanded view shows it in the track meta line when present.
- **2026-08-29 — Quick-wins pack.** Artist name on its own line beneath the expanded-view title (moon-bright ink) with an album · year · BPM meta line; volume control on the right of both transports (mute button + accent rail with 11px knob, persists in settings, preview-fade safe); Windows taskbar integration (progress fill, prev/toggle/next thumbar buttons, flash on track change); window title announces now-playing; accent-reactive toasts with the song title emphasized in display type; session resume — restart lands paused at the saved position with the queue intact (kv force-flushed on quit, position saved every 2.5s).
- **2026-08-29 — Phase 3 v2 concept locked: "The Particle Lantern" (not yet built).** Born from a live observation: during v1, the mote canvas kept rendering in the dead zones where the cursor image vanished — because canvas pixels are never subject to OS cursor suppression. v2 inverts the architecture: the pointer is *drawn*, not declared. Zero OS cursor — `cursor: none` applied permanently through the high-specificity rule (inputs keep the native I-beam yield; the v1 flicker was our own bitmap swapping, which this design cannot reproduce). The pointer's face is one pre-rasterized sprite — the owner's SVG art, rendered once per accent into an offscreen canvas at devicePixelRatio scale, accent-tinted via source-in — stamped synchronously at the last-known pointer position every frame: one big always-on particle that never fades, guaranteeing exact pointing precision. State morphs (hover-converge, press, edge chevron) are swaps between cached sprites — same-frame, no OS involvement. The existing mote system provides the movement trail (palette memory, caps, drift). Honest trade: canvas paints on the main thread, so heavy load freezes the whole formation for a frame — masked by the soft glow + trail, and nothing native ever appears to contrast against it. Verified groundwork: the v1 mote canvas demonstrably rendered in every zone where the cursor image was suppressed (drag regions, window bounds). Owner to supply per-state SVG art; motion-flag gated as before. Build order when reopened: sprite pipeline → pointer-follow core → state morphs → trail hookup → text-yield → flag. **Drag spike verdict (2026-08-29, same day): the Win32 shortcut is dead.** All three native move-loop initiators — `SendMessageW(WM_NCLBUTTONDOWN, HTCAPTION)`, its `PostMessageW` form, and `PostMessageW(WM_SYSCOMMAND, SC_MOVE|HTCAPTION)` — were delivered successfully to the frameless window (verified by return values and koffi FFI) during physically-driven drags, and Chromium ignored every one: the native move loop never engaged, the window never moved. Manual repositioning (setBounds polling, prototyped earlier) is therefore the *only* topbar path for v2; the alternative is living with the native arrow on the drag strip. A `no-drag` "gear island" already patches the worst seam. **Decision (2026-08-29, owner-confirmed after studying osu!'s screen modes): adopt the osu! windowed-mode trade — the drag flag is removed from the top bar entirely.** The window is not mouse-draggable anymore (screen-mode settings — fullscreen/borderless/windowed — become a future rider), and in exchange the particle cursor owns every pixel of the window with no arrow, no dead zones, and no flicker. Resize via the frame's invisible edges still works. The fade-out detection for `.window-drag` zones remains in the test code and self-disables now that no drag flag exists.
- **2026-08-29 — Cursor experiment settled; native top bar restored.** After living with a minimal particle-cursor test build (throwaway `particle-test.ts`: permanent `cursor: none`, one 60px accent-tinted glow stamped at the pointer every frame, F8 toggle), the results were encouraging — the particle rendered flawlessly in every zone where v1's native cursor died, including both bars, and the chip flicker was confirmed structurally gone — but the drag-region arrow remained on the top-bar strip. Weighing the options (live with the arrow, hand-rolled dragging, or borderless fixed-size), the owner restored the native top bar: drag flag and maximize button reinstated, native double-click-maximize back. The particle test build remains in the tree behind F8 for future feel-checks, and the v2 concept note stands as the map for a proper build. Verdict of the day: a canvas cursor is immune to every suppression wall, but cannot be zero-lag; a native cursor is zero-lag but suppressed in exactly the places a music player's chrome lives. Phase 3 stays deferred until both halves of that sentence can be true at once.
- **2026-08-29 — Follow-up pack: window memory, drag-strip refinement, preview discipline, volume geometry.** The window now remembers its last position, size, monitor, and maximized state across restarts (normal bounds stored via `getNormalBounds`, saved on close into the flushed-on-quit store; dead-monitor fallback discards saved bounds that intersect no display instead of opening off-screen). The top-bar drag strip now toggles with window state — present and native-arrow'd only in windowed mode, entirely absent (particle cursor owns the strip) when maximized or fullscreen. A hover-state leak was fixed: tab switches used to strand pending dwell timers, active previews, and the paused main song (Chromium never fires `mouseleave` for removed rows) — every content re-render now cancels preview state, resumes held playback, and pops the accent theme; hover previews are additionally gated to the Songs tab only. The volume cluster was refined: the speaker button sits equidistant between the total-time readout (now right-aligned) and the full-length rail, with an 11px knob on an enlarged hitbox.
- **2026-08-30 — Phase 3 v2 shipped: "The Particle Lantern" is the cursor.** The throwaway particle test was rebuilt properly as `core/lantern.ts` and the seed retired: permanent `cursor: none` (inputs keep the native I-beam), a sprite pipeline (per-state registry holding owner-SVG slots + a generated glow stand-in, rasterized per accent at devicePixelRatio, hotspot-anchored), and a pointer-follow core that moves a small composited canvas by `translate3d`/opacity instead of clearing a window-sized bitmap — the seed's latent resize double-scale bug, its pointer-exit ghost, and its never-parking loop all died with it. The accent chase re-reads the root's computed `--acc-a` across the full 700ms window (`tokens.css` transitions the registered colors on `:root` itself, so write-time reads return the transition's start value — that was the "color lags / defaults after restart" bug); the cursor now glides through song changes in sync with the UI. Drag-strip handling is event-driven: the pointer event's own target replaces per-frame hit-testing, a class watcher on `#topbar` re-checks when the strip toggles, resize re-checks. Timeline: fullscreen/maximized is particle-owned (draggability removed there), windowed keeps the native drag strip — accepted trade. `fx.lantern` registered at birth under the master motion switch; F8 stays as debug toggle until the settings row lands; kill switch restores the native cursor. Latency model banked: same-frame sampling + present ≈ 1–2 refreshes — as fast as a drawn cursor can be.
- **2026-08-30 — Dust motes, click bursts, and the light ribbon.** A trail canvas sits beneath the cursor sticker: a fixed 28-slot ring buffer (zero allocation while animating, oldest recycled at cap) of loose-cloud motes — round blobs (4–8px, stand-in for the small lantern) mixed with generated four-point stars (6–10px), six pre-rendered color variants per type derived from the song's accent (hue-shifted ±14°, darkened to 40–66% lightness — always darker than the cursor), spawned budgeted by distance traveled and *flanking* the path 3–14px to either side with an outward sideways kick (`MOTE_FLANKED` reverts to centered spawn). Clicks burst 10–14 radial motes; everything is suppressed over the windowed drag strip. The light trail is a redrawn polyline (14 points, 0.45s memory) tapering 6.5→1.2px and fading with age, in the same pass on the same canvas, clearing only the union of where motes and ribbon were last frame; the loop parks at zero when everything settles.
- **2026-08-30 — Lag root-caused and fixed (performance campaign II).** The washes were the monster: `palette-field`/`overlay-field` animated `background-position` through a 70–80px blur — full repaint + re-blur at 60fps forever — while band-driven `saturate()` forced another re-raster at every band update. Both now drift by compositor translate (`field-drift`), keep blur/saturate static per palette, and breathe through opacity and scale only (sigil's band-driven drop-shadow radius made static). The transport no longer re-layouts the page each frame: time text writes only when the string changes, scrubber fill/knob quantize to 0.1% steps. The band bridge updates its root custom properties every other frame with compensated smoothing (0.48/0.39/0.33 vs 0.28/0.22/0.18 — identical response, half the writes); the visualizer stays full-rate. Lessons banked: band-reactive filter *functions* re-rasterize the whole filtered layer — pulse via opacity/scale, never filter values; never animate background-position on blurred layers.
- **2026-08-30 — Bug batch and lyric settings.** Expanded-view meta line rides `--moon-bright` at 13px. Single-song albums open the stage again (owner toggle `openSingleStage` at the call site). Lyric pre-roll rests at the raised scroll floor (35% → 15%, just under the top fade) so early lines start high and the first timestamp no longer jumps. Same-track `setTrack` re-emits the resolved state instead of returning silently — replaying the loaded song from any view no longer strands the expanded view in Art-only. The `settings:update` whitelist gained `timelineLyricSize`/`lyricAlign`/`lyricPane` — new keys silently refused to persist without it, which was the "dead buttons" (root-caused via CDP round-trip, not by eye). New settings: lyric text alignment (left/middle/right), lyrics pane side for Art+Lyrics (right/left, borders and padding mirror), and a separate timeline-lyric S/M/L (`--timeline-lyric-scale`). Lyrics Only column widened 760→1080px via a shared `--lyric-col-w` that also drives the edge zones. View dots enlarged to 12px inside 24px targets with doubled spacing; the locked-dot reject-shake finally got its CSS rule.
- **2026-08-30 — Settings and lyric-position refinement pass.** Lyric pre-roll now rests at the line-one *anchor* position — the same height active lines occupy during normal listening — instead of the fade-hugging 15% floor, so start and normal display are one height and the first timestamp causes no jump. Settings modal: card widened 540→640px, the dim backdrop gained a real 14px frost, and the card gradient raised to near-opaque. Lyric alignment labels shortened to L/M/R; the Timeline lyric size row moved above the text-size control, renamed "Fullscreen View Text Size". The on/off toggles no longer summon the native pointer: the lantern's `cursor: none` is now blanket, with the native I-beam re-yielded only on text-entry inputs (checkboxes were swept up in the old wholesale input exemption). And the master effects switch no longer kills the pointer — the lantern's pointer core is decoupled from the master switch (toggling ambient effects no longer flashes the native arrow); master and the future granular flag gate only the motion effects (mote spawning, ribbon sampling, bursts), while F8 and the coming motionLantern setting remain the pointer's off switches. Root-caused and verified via CDP round-trips.
- **2026-08-30 — The playing-row marker: owner art arrives, and the moments primitive with it.** The Songs view gained its committed-state marker built from the owner's own artwork: a multi-tone arrow (delivered as white-on-black PNG) loaded through the new asset pipeline (esbuild `.svg`/`.png` file loaders, `assets/` in the renderer tree), converted at load — the opaque black background is corner-detected and turned into transparency via luminance-to-alpha — auto-cropped to its drawn bounds, and re-hued per playing song (uniform rich accent, shading carried entirely by the preserved alpha; re-tints through the accent chase, debounced). The pair flanks the playing row **outside** it, mirrored, vertically centered, sized 64px, and sheds mixed round/star motes: ~3 per arrow per second streaming **backward** (outward, 60–150px travel, spawn weighted toward the arrow body, varied lifetimes and vertical drift), bursting 4–6 strong on musical moments. Those moments come from a new shared primitive, `core/moments.ts` — an onset detector on the raw band energies (30Hz positive-flux against an adaptive rolling threshold, 200ms rate limit, strength-scaled, pulse-gated, driven by the player tick): the charter's "moments, never loudness" made real, and Phase 7's playhead particles will consume the same signal. Mote budget rose to 40; mote sprites were rebuilt larger with baked two-stage bloom (stars: blurred halo + mid layer + sharp star + white core; rounds: wider four-stop falloff). The row enlargement idea was explored and retired by owner decision — the arrows carry the committed state at 96px.
- **2026-08-30 — Marker lag root-caused; arrows go static; hover decoupled from the palette.** The bob was the lag: two drop-shadow-filtered markers repainting every frame, whose layer area had grown ~19× when the triangles became the owner's art — blur cost scales with area. The arrows are now **static** (owner request), rasterizing once; the re-tint churn is debounced to a single re-tint after 750ms of quiet (hovering the list no longer repaints the markers at all), and the multi-step high-quality downscaler replaced a single-step aliasing resize. Separately, hover no longer recolors the UI **anywhere**: the palette push/pop wiring on song rows is removed, and the accent chain answers only to the playing song (audio hover previews unchanged; the CSS hover highlight remains, now static in the playing song's color). Verified via CDP round-trips and frame-budget reasoning; the retro is banked as a lesson: never animate a blur-class-filtered layer without measuring — cost scales with layer area.
- **2026-08-31 — Movement IX chartered: "The Crystal Atlas" (`docs/charter-movement-ix.md`).** The browse surface goes three-dimensional across three views: Artist (Albums and Artists tabs merge into a cover-flow shelf opening onto year-mapped discography charts — earlier = closer), Song (the flat list replaced by a perspective plane of crystal slabs rising from the horizon haze), and Constellation (the playlist successor: songs plotted as draggable star figures over the inherited KV store, design-doc-first). The state grammar gains a Z axis — **solid means committed, dotted means preview, near means NOW** — and depth replaces the arrows: the arrow pair and the flat-list hover glow retire, while moments/motes, the pause-with-resume audition machinery, and the peaks pipeline are absorbed (previews become a near-zone privilege; every crystal face carries its waveform silhouette; the compass pattern — a horizon star that returns the view to the playing item, never auto-following — generalizes across views). Material law: crystal is painted, never frosted — `backdrop-filter` banned in 3D contexts, three faces maximum per slab, accent-chain tinting only — and the aesthetic contract is amended to v2 inside the charter. Phased: merged top bezel first (wordmark removed; tabs, summon-search center, contextual sort/filter marker, status + gear island in one row), then the throwaway crystal-slab spike as the movement's escape hatch, then Song, Artist (after a year-tag audit), Constellation. Library-derived only for now — **no external APIs — yet** — with the cached settings-gated artist-dossier tier banked as a rider. Same-day provenance: an earlier atlas-language experiment (corner brackets, star dividers, de-glassing) shipped and was reverted within hours — brackets read as sci-fi HUD, not cosmos — and its lesson is folded into the charter's material law.
- **2026-08-31 — Phase 1 — The Bezel signed off.** The merged single-row top chrome landed after three same-day live-iteration rounds with the owner: one 52px bezel replaces ~140px of two-row chrome (wordmark and status line retired — the gear island owns library status; ~90px returned to the sky). Tabs are de-boxed tracked-caps text with the active view flanked by accent stars; view options open from the active tab, with the sort/filter system removed pending owner reconsideration. The summon sits center in a reserved slot — crescent-and-sun glyphs (owner-tuned), star-swap input, plate-styled results panel with no frost and no count readout. Above the tabs, a meridian starfield: sparse varied-brightness specks twinkling in place plus rare slow travelers — the charter's "reads as space, never dust" ruling — with the twinkle compositor-accelerated after a probe caught var()-in-keyframes forcing bursty main-thread wakes; gated by the ambient motion flag. The deep-field fade dims the sky beneath the hairline; the bezel is the zenith of the app's space. Two experiments were scrapped en route and are recorded in the charter: the graduated instrument edge (owner found it noise at this scale) and the arrow-art tab marker (the multi-tone art loses its body at 16px). Separately, the drawn-cursor freeze on focus loss was root-caused with CDP probes — renderer measured flawless during movement (~7ms median, zero hitches >25ms, starfield on or off) while covered/unfocused states starved rAF to zero frames — and fixed with `backgroundThrottling: false`, the correct posture for a living particle UI. Owner/second-agent fixes landed: summon glyph colors, stray arrow particles on non-Songs views, arrow tint latency.
- **2026-09-02 — Year-tag audit passed (Movement IX Phase 4 prerequisite).** A full sweep of the library index: 45 tracks / 26 albums, **100% year coverage** — every album carries exactly one clean year (range 2014–2026), zero conflicts, zero unknowns, no untagged tracks. Verdict: the Artist view's discography-by-time chart (earlier = closer) is fully viable on the current library — Mili's nine dated albums are the showcase spread. The charter's "uncharted cluster" fallback for unknown years stays in the design as future-proofing but is empty today. Next step on resumption: a design pass on the Artist view itself (shelf + discography chart, albums absorbed, old Albums/Artists tabs dormant until parity).
- **2026-09-02 — Handoff state (owner-requested, ahead of limits reset).** The Song view ("river", `ui/songRiver.ts` + `styles/river.css`) is the live Songs surface — flat list dormant via `body.song-river-active` CSS; swipe-right opens floating playlist names (bare ink, inline "New playlist" input, duplicates marked), swipe-left appends to queue (deduped via `swipeQueued` until the song plays), mote showers confirm actions, wheel anywhere scrolls, double-click outside snaps back to the playing song; scroll physics reworked and the tab sort dropdown removed by second agent (commits `e77ea7c`, `0a5e12e`). Lyrics resolver now escalates to `/api/search` when exact-match returns plain-only (commit `1c99d8d`) — unsynced phantom fetches self-heal to synced and are written beside the file on next resolution ("Looping the Rooms" verified end-to-end; no unsynced `.lrc` exists on disk — they were never files). The loop-button/volume-icon experiment was **reverted by owner decision**; the only uncommitted change is the `settings:update` whitelist gaining `'volume'` + `'loopMode'` in `main.ts` — keep `'volume'` (fixes genuinely dead persistence, verified), `'loopMode'` is inert until the loop returns. Known leftovers: test volume left at 0.77; silent river tuning keys (↑↓ tilt · ←→ fade · shift+↑↓ depth) still in code pending sign-off; sort popover still opens on tab clicks (flagged, undecided). **Standing rule: no commits without the owner's explicit approval.**
- **2026-09-03 — Movement IX Phase 2 passed: the spike became the river, and the river is committed.** The throwaway slab spike opened as an F9 stress rig (painted slabs vs flat panes), then evolved through curved-slab experiments (visible side faces, bow curve at both ends, vanishing point under the bezel) into the vertical curved river — real art, click-to-preview/commit, wheel scroll. It graduated to the live Songs surface (`ui/songRiver.ts` + `styles/river.css`): 21 painted slabs riding glide physics between chrome-pinned edge fades, a center glow behind the focused card, and the committed card wearing a full accent bloom; swipe-right opens the floating playlist picker (bare ink names, inline "New playlist" input, duplicates marked), swipe-left queues the song (deduped until it plays), mote showers confirm actions, double-click outside snaps back to the playing song, and the wheel scrolls anywhere on screen. The flat list is dormant, not deleted — hidden behind `body.song-river-active` until Phase 3 parity retires it. **We have tested the new song display system and found it superior to the previous UI.** The hidden tuning keys (arrows adjusting tilt, fade, and curve) and the spike-era fake-title fallback were removed from the code after the verdict. Open rider: the tab sort-popover question remains undecided.
- **2026-09-03 — Summon center pack.** The summon results are keyboard-drivable (arrow keys move the highlighted plate), and summoning now spreads a dim veil over the world below the bezel — the summon field and results panel stay lit while everything else fades 420ms opacity-only, and outside clicks land on the veil so the world pauses during a summon. Renderer-only; no settings, IPC, or stored data touched.
- **2026-09-04 — Transport: loop toggle, unified moon-white ink, river cleanup.** The bottom transport gained a binary loop toggle — dim moon at rest, bright ink when on, repeating the current song indefinitely; the choice persists across restarts through the settings whitelist, and skip always overrides. Loop-once and infinity variants were explored and retired by owner decision (the "1" badge died of illegibility; the ∞ of flatness). The speaker glyph grew to 22px and the whole icon row settled into one non-reactive moon-white family — the selector-bar lesson in reverse. The river shed its spike leftovers: fake demo titles and the hidden tuning keys.
- **2026-09-04 — The Summon: the word that fades into the cursor.** The summon center was rebuilt around the original trio — crescent, word, sun — now alive. At rest: `—— ☾ SUMMON ☀ ——` in the playing song's tint between two fading hairlines. A click or keystroke parts the pair and the word fades into the blinking caret, whose stage is never smaller than the word's footprint — the gap is max(word width, text + cushion), so the frame never flinches when the first letter lands. Typing snaps the gap to size; only open and close glide. The whole row is one target: click or hover any part — moon, word, sun, lines — and the entire composition glows together, the lines easing outward under stable pointers only (a line hovering itself oscillated and was retired). One song tint rules the row, living on the zone so every child inherits it — the eclipse refactor had stranded the sun outside the tinted button, and the owner's eye caught the two-tone family within a day. The drawer hangs flush from the bezel: square-cut, its single 1px top hairline built from the bezel's own edge formula (matched colorimetrically, white bloom at center), its fill wearing the same horizon washes as the zenith. Search results left the line era: each hit is a frameless strip of its album's art — side fades into the void, a per-row crop shift breaking same-album repetition, dark scrims carrying song-tinted titles, the keyboard selector an accent bar speaking in unison with the whole panel (isolated reactivity was the jar; cohesive reactivity is alive). Section headers centered — ── SONGS ──. Lessons banked: the word had been measured for days but never painted, because it lived inside a zero-width overflow-hidden field — geometry checks pass where visibility checks fail; boot-time text measurements must re-run on fonts.ready or they drift by the width of the missing font; and a hover target that moves itself oscillates — drift only under stable pointers.
- **2026-09-05 — The Listening View experiment: built, lived with, reverted. The flanks stay void.** The side-panel charter (`docs/charter-listening-view.md`, v1–v3) played out uncommitted end to end: overlay docks (playlists left, queue right) over the river's flanks; near-black glass with 14px blur; the queue panel relocated and the transport's queue button retired; then the in-place drill (the panel itself turning into the playlist's songs, art-chip rows, ESC to climb back) and throw-to-summon — card throws peeking and summoning the panels, side-swapped to the zone grammar (left throw = file the song, right throw = queue it), with the floating picker, swipe prompts, and gradient circles retired. On live review the owner reverted all of it to the river commit: **"lost the cosmic feeling by feeling too generic and too much like a spreadsheet rather than the cosmos."** The bugs concurred (queue geometry grazing the timeline; a left throw that read as "create playlist"). Banked law for Movement IX and beyond: **the river's power is the void around it — context surfaces must be momentary and cosmic in kind (the summon drawer is the model), never docked player chrome.** The complete v3 implementation survives in git stash ("listening-view v3 full snapshot"); the charter remains on disk as a rejected-experiment record. Marginal keeper re-applied if ever wanted: the playlists store's load dedupe.
- **2026-09-06 — Movement X — The Universe: chartered, and the spike took shape.** Chartered (`docs/charter-movement-x.md`) as the project's sole active focus after the owner's verdict that "the cosmos is decoration on the chrome — the sky must be the place." Governing sentence: **the universe is where you choose, the river is what you see, the queue is what you hear.** The spike lives on branch `spike/universe`: a temporary UNIVERSE tab opens a full-bleed night sky with a parked-rAF camera (drag pan, cursor-anchored wheel zoom). Stars = one per primary artist, sized and placed by song mass (log weight bands with radial jitter), swept alphabetically around the heart by letter-proportional arcs, rim-lit orbs tinted from their album palettes; elliptical contour rings mark the weight bands and scale with library growth. Albums pin to concentric rings around their artist (cap eight, "The ring holds eight"), persisted; playlists are four-point stars sized by song count, hue-blended from their source artists, placed at the midpoint of the stars they pull from; empty playlists are ghost sparkles. Clicks fall inward: star → Albums filtered on the artist, satellite → album, sparkle → playlist. A scanner upgrade parses artist credits — the first name before `;` `,` feat/ft owns the song (owner-confirmed table), a safety pass catches featured-first tags, `INDEX_VERSION` 6 rescanned once — so "Mili; Kihow" belongs to Mili's star while the full credit stays visible and Kihow stays searchable. Album pinning via a stage button; "The ring holds eight" toast guards the cap.
- **2026-09-06 — The repaint: black, not navy; chrome of fog; and the timeline's true enemy.** The void tokens lost their navy cast and the ink its lavender (root tokens + 42 rgba literals + 35 hardcoded navy literals swept to the tokens); the corner ambient washes died; the universe went full-bleed behind a translucent bezel and transport (chrome raised above the sky, painted alpha — no frost over animated content); layers dimmed via a unified veil (86% over content, 62% over the universe); nebulae and the summon rows quieted — dynamic color confined to the bezel junction and the selector bar. Then the timeline: after four rounds of "fixed" that the owner's eyes correctly rejected, DPI-aware physical capture found the true enemy — **the Windows 11 secondary taskbar overlays the app's bottom edge** (work areas unreserved), hiding everything the instruments kept "verifying." The window-state restore now clamps to the work area as a guard. With the taskbar settled, the transport was rebuilt by measurement: fill fog removed, rail a 3px moon strip (78% played-side presence, 22% unplayed quiet), pane glow plateaued and widened, fog removed above the hairline and strengthened below it, and both hairlines muted to quiet colored glints (207/173 → 80/98) — the brightest things in the app are content again. The summon drawer stopped competing (rows neutral, dynamic color confined to the bezel junction and selector), the timeline-lyric S/M/L grew onto its own scale (20/25/31px) behind a feathered void scrim, and the lyricless expanded view hides its mode dots without moving a pixel. Standing laws banked: verify visual claims on the physical screen (DPI-aware), never on internal buffers; translucency is painted, never frosted over animated content.
- **2026-09-10 — The Universe rebuilt on the MKII star: bare sky, one star ported, two fixes proven live.** With the star recipe matured in its own repo (MotiveMKII, owner-reviewed v1→v9), the universe view was stripped to a clean slate — every sky object removed (stars, labels, halo/pool canvases, sparkles, black hole, threads, field dots; 1,445 lines), leaving the surface, the parked pan/zoom camera, and the placement math as exported pure functions (`scripts/verify-bare-sky.mjs` proves empty void by pixel scan). Then slice 1 of the port: the recipe copied verbatim (`renderer/universe/starShader.ts` + `starParams.ts`, comments stripped, strict-safe), a GL renderer module (`renderer/universe/starfield.ts` — per-star draw region, additive light, clear to `--void-0` not black, camera → screen position + pixel radius), and a minimal identity adapter: playing artist (largest as fallback), chroma-weighted palette tone, mass from song count, albums oldest-first driving the orbiting motes. Verified by `scripts/verify-one-star.mjs`: peak light, drawn hue within 1° of recipe expectation, zoom/pan tracking. Two live-use bugs then found by the owner and fixed with measurement: (1) **motes panned at 2× the star** — the shader projected companions from the world origin, carrying the star's own screen offset, so the orbit center sat at twice the star's offset from canvas center (invisible at rest: the star parks dead center — center-only tests were structurally blind to it). One-line projection fix landed in MotiveMKII (`e207ed9`) with an off-center regression test (demo hero gained cx/cy placement; suite 21 checks), ported here; live pan ratio now 0.99×. Banked law: a projection like this is exactly zero at screen center — regression-test off-center. (2) **the glow's distinct edge** — radial profiling found a log-slope cliff at ~3R (both corona lobes exponent 6: inner died where outer was flat; knee jump 4.12) plus a dithered 1/255 plateau with a hard rim to ~4.6R that read as a disc edge over the void. MKII v9 (`cc85c96`): corona rebuilt as one exponent-4 lobe under a smooth extinction envelope (`exp(-pow(r/4.6R, 2.2))`) walking the tail through every quantization level to 0 — knee 0.73, no plateau, extinction ~4.5R. First cut drifted far-zoom hue to the tolerance edge (the fat old corona had been counterweighting the heart's +6); amplitude/tint rebalanced until the full wheel passed — suite now 22 checks including a corona-tail profile guard. Ported and live-verified: the glow decays smoothly into the void floor by ~4.8R. Owner verdict after living with it: "much nicer," and the motes now orbiting just outside the tighter glow read as countable album satellites against the void — accepted as the better composition (per-artist orbit radii keep inner motes grazing the halo, so systems stay cohesive). Standing: the recipe repo holds v9 + 22-check suite as the star's home; the app consumes it by verbatim copy.
- **2026-09-11 — The river measured, the shimmer fixed, River v2 chartered as a spike.** A measure pass on the live river (CDP transform traces, Performance metrics, pixel bursts) named the slow-scroll shimmer's root cause: motion values quantized coarser than a device pixel of visible effect — scale rounded to 0.01 steps (5.2px pops on a 520px card), oscillating at rounding boundaries every 1–4 frames at slow scroll. Fixed by continuous writes with position on the device-pixel grid (`cf3368a`); Layout/Style/Paint measured 0/0/0 throughout and idle parking verified. Banked law: round position to device pixels, never round scale/tilt/opacity — the pops existed at every refresh rate, so the fix is universal, not display-tuned. The browser was exonerated (compositor-cheap at this scale), but three findings were recorded as the rebuild's evidence base: the fling ceiling (~61fps on a ~170Hz display), the coupled layout constants (visible count ≈ viewport height ÷ 145, not a parameter), and the visible recycle pop (~144px / ~36px when a ring card rebinds). River v2 was then chartered as a spike on branch `spike/river-v2`: a standalone `sandbox/river/` lab (the sandbox/universe pattern, real tokens and fonts) with a contract-first design (`mount(region)`, `setEntries`, a reversible `reveal`/`collapse` timeline), seven requirements each carrying an acceptance test — parametric layout, zero shimmer, preserved glide feel, art + artless card recipes, the fling performance bar, byte-stable reversible entrance, invisible recycle — and a DOM-vs-canvas slab feel-off as the first evidence-gathering step. Full brief: `docs/lab-brief-river-v2.md`. Banked for the future artist-view transition (out of the spike's scope): star anchors left with the artist's name above it; motes flash, fly right as one group, and become the first visible entries newest-album-first, the rest staggering in; exit runs the same machinery in reverse.
- **2026-09-12 — River v2: the rebuild lives in the app, and it plays.** The spike's delivery vehicle changed by owner ruling before any code: no standalone sandbox — the new river is a hidden in-app view (F9 swaps it into the Songs slot over the real chrome; the live river is untouched behind `body.river-v2-active`; real library data; probes through the app's own CDP port, so the comparison against the ~61fps fling baseline is apples-to-apples). The surface (`ui/riverV2.ts`, contract factory: `mount(region,dpr)`, `setEntries`, `glideTo`, `entryRect`, `hideEntry`…) derives all geometry from region × visibleCount × a depth dial: a self-similar depth walk (spacing follows size and tilt-foreshortening from one table), a center lens derived from the slot math, a continuous Shorten squash near list ends (owner ruling: NOW=center — edges compress symmetrically instead of showing asymmetric stubs), an edge dissolve built as an alpha mask on the river layer (dissolves into whatever is behind — void today, the starfield tomorrow), small-set grammar (one song centers; two straddle symmetric; sets smaller than the band are pinned), wheel/drag/fling physics ported verbatim from the live river, and double-click-on-void glides home (the target is re-resolved id→index at click time, so it finds the song even if the queue moved while away; the river stays put on external track changes — owner ruling). Click-to-play: tap-vs-drag through one shared pan helper, `entryActivated(id)` commits via `setContext` with the full library as the queue, the committed card wears the live river's accent bloom recipe, and replaying the playing card opens now-playing. Two measured bug stories banked: the owner's "jumps up and down near the middle" was a dead zone in the depth-table lookup — a `floor()` fast-path pinned every card within one step of center, popping 7px at each song boundary (found by tracing rendered rectangles per frame; the position variable was provably smooth — pixels and state diverge); and the void hit-layer under a `pointer-events: none` root was input-dead until it declared `pointer-events: auto` itself. The probe (`scripts/probe-river-lab.mjs`, 40 checks through the app CDP) now asserts motion continuity the honest way — a card may never move further in a frame than the scroll position itself traveled — and starts from a neutral position so runs don't inherit each other's playing state. Open: card recycling (all slabs painted; fine at 158), reveal/collapse + the spread-exit timeline, the fling fps bar unmeasured, DOM-vs-canvas verdict (DOM default).
- **2026-09-12 — Palettes learn prominence (neutral lockout fixed).** Owner-reported with a specimen: usedcvnt's *ultraviolet* — subtle grays carried by purple/pink/dull-red — rendered the hardcoded moonlight pair (cyan/blue timeline). The stored palette was three grays: the old extractor (32px sample, 4-bit buckets, 3 slots by raw population) let neutrals lock out every chromatic tone, and the chroma-only runtime pick never had a chance. Rebuilt as a prominence pipeline: extraction samples 64px at 5-bit buckets and picks two lanes — population (up to 4) plus a chromatic reserve (up to 2, ranked chroma × √share) — storing each tone's pixel share; `INDEX_VERSION` 7 forced the one-time rescan (158/158 tracks carry palettes + weights). Runtime `selectTone` replaces chroma-only selection everywhere: candidates clear the same neutral-vividness floor (truly neutral art still honestly falls to moonlight — the 2026-08-27 law stands), then score chroma × √prominence, and the weights thread through the whole color system — accent chain, horizon haze, lyric ink, arrow-marker tint, live-river swipe accents, album scopes. Acceptance measured: ultraviolet's palette went from three grays to four neutrals + muted purple `#725c75` + lavender-pink `#a48caa`, and playing it writes `hsl(293)` with the timeline's chromatic pixels moving from the cyan/blue band into violet-purple. The prominence exponent is the taste dial if any album over/under-weights its fields. Banked: a palette is colors AND their areas; a neutral floor can only judge tones the extractor guaranteed to exist.
- **2026-09-12 — The song menu: an intent layer, a panel that lives inside the river, and a hover preview promoted to feature.** Three builds in one arc. (1) **The intent layer** (`core/songActions.ts`): surfaces emit, stores act — `queueNext` (plays-next insert, deduped against the live queue), `fileIntoPlaylist` (the store's duplicate guard now *reports* its verdict instead of failing silently), `createPlaylistWithTrack`, `removePlaylist`, `queueRemoveTrack`, `queueMoveToGap` (the player's move is GAP-semantics — `to` is the insertion gap and `to === from + 1` is the no-op drop — the layer now says so by name), plus the reads the menu consumes. The player gained `insertUpcoming` and a public `queuePosition`. (2) **The menu component** (`ui/songMenu.ts` + `styles/songMenu.css`): one state machine — fork (*Add to queue · Add to playlist*) → the **live queue view** (compact art-strip rows, the landed row pulsing accent, drag-to-reorder with insertion line and gap semantics, hover ✕ removal) or the **playlist list** (*New playlist* with a summon-style typing field, muted duplicates, hover ✕ → **Confirm | Cancel** → **"Deleted"**); every terminal state speaks one accent word — "Added"/"Deleted" — then fades; dismissal is scroll-outside, outside click, or Escape (typing-Escape backs out first). Mounted on the v2 river as a **child of the right-clicked card** — it inherits the card's transform, so it glides and warps in lockstep with zero independent motion; right-click arrives through a new river-contract callback (`onEntryContext`) with a sliver guard (cards projected under 24px tall decline — the probe caught a 203×16.6 edge card). The card's `overflow: hidden` was clipping the whole panel into nonexistence — `.rv2-card` is freed and `.rv2-art` clips itself now. The queue-phase drag: no HTML5 drag, pointer-captured rows, window-tracked moves. (3) **The summon hover preview promoted from accident to feature**: previews were discovered alive on summon rows (inherited row-species wiring) but gated to the Songs tab — now one helper (`syncPreviewEnabled`) enables previews when the Songs view is active *or* the summon is open, the previewing row wears a subtle accent-chain glow (the flat list's own preview styles turned out to be stale hardcoded teal), and `closeOracle` cancels preview state — plugging a real leak where Escape mid-preview stranded the ducked main track. Verified by three new probes (`probe-song-actions` 10 checks incl. live queue surgery, `probe-summon-menu` 15 checks incl. synthetic drag and the Confirm/Cancel flow, `probe-summon-preview` 6 checks on a non-Songs tab); all gates green, 37/37 tests. Design on record in `docs/charter-song-menu.md` — **including the pending next session: panel sizing (width +25%, height = the full card), right-click reliability (trigger on press, not release; sliver guard relax; same-card toggle), and the chrome mount (summon rows + stage cells + transport button in queue phase; `queuePanel.ts` retires).** Cosmology recorded: the queue is not a place (no star, no motes — motes are albums); the universe's black hole = doorway to all playlists AND the virtual master playlist; the universe-star delete rider and the swipe grammar retire (the menu is the fast path now). Banked: inheritance beats simulation; assertions inside projected 3D space must be projection-aware; a CSS-hidden surface is still alive; synthetic clicks blur focused inputs after the handler — real mice don't.
- **2026-09-12 — Tabbing away from Songs no longer pays a dead 240ms.** The owner noticed leaving the Songs view was slower than every other tab change, and suspected the new river. Measured with a swap-timing probe (click → destination content, three runs per direction): Songs→Albums sat at ~250ms under both rivers while every other direction was 3–25ms — v1 and v2 identical, the river exonerated. Root cause: `render()` carried a flat-list-era special case — leaving Songs retracted the visible song rows (a staggered farewell) and waited a hardcoded 240ms before rendering the destination. With a river owning the Songs view, those rows are hidden behind the dormant `body.song-river-active` list, so the app performed the farewell for nobody. Fix: the retract-and-wait gate now requires the flat list to actually be the visible surface (neither `song-river-active` nor `river-v2-active` set); river-owned swaps run immediately. Songs→Other dropped to 5–22ms, all directions uniform; the flat list's choreography is preserved for whenever it is the surface again. Instrument: `scripts/probe-tab-swap.mjs`. Banked: choreography that animates a hidden stage reads as lag — swap gates must check what the user can actually see.
- **2026-09-12 — The river-v2 lab stops haunting other views.** Owner report: the F9 river showed over Albums and Artists, but not the Universe. Reproduced by pixel-counting a viewport sample across tab switches with the lab armed: Albums 91% river-bright, Artists 84%, Universe 1.9% — the river stayed `on` everywhere. Root cause: the lab broke the house visibility pattern — every full surface is driven by browser.ts's render (the live river via `songRiver.setVisible(mode === 'songs' && …)`, the universe via `setUniverseVisible`), but the lab answered only to F9. The Universe "worked" by accident: its z-25 sky merely covered the z-16 river, which kept rendering underneath (and could bleed through the summon and detail veils). Fix: the lab exposes `setBrowserVisible` ANDed with the F9-armed flag, driven by `syncRiver()` with the identical condition — F9 arms, the browser decides. Verified across all four views (display:none on Albums/Artists/Universe, round-trip back to Songs restores the river without re-arming) by `scripts/probe-v2-views.mjs`.
- **2026-09-12 — The timeline stops jumping, and the summon hands you to the river.** Two owner-reported items, both measured before touching anything. (1) **The normal↔expanded view swap had a subtle jump in some transport elements.** Root cause found by rect-dumping both strips across the swap: the two views run separate transport DOMs (`#bottom-bar` vs `#overlay-bottom`), and `#bottom-bar`'s 1px `border-top` consumed one pixel of the shared 62px strip height under border-box sizing — so every content element's centerline sat 0.5px lower in normal view (buttons 1026.5 vs 1026, scrub 1027.5 vs 1027). At 1× DPR each element snaps individually, so only the ones crossing a pixel boundary visibly jump — "some elements, not all." Meanwhile the timeline's hairline didn't move on the swap — it vanished: the expanded strip is deliberately borderless (its bright, pulse-swinging wash measured 29–98 luminance depending on playback state — a copied hairline would have read as a dark seam at best). Fix honors the borderless ruling: the border came out of the box, and the floating `::after` bloom was promoted to the full line painter (the old border color stacked as a base layer beneath the gradient), repainting the identical row at the identical pixel while both strips become the same 62px content box. The jump is dead; the expanded view was touched not at all. (2) **Summoning a song now glides the river to it.** First cut called the v1 river directly — thorough investigation with the F9 lab live exposed that the old river under `body.river-v2-active` is only CSS-hidden (its `on` flag stays true), so the summon was gliding an invisible river while the visible v2 never heard about it (reproduced: clicked song at index 89, river parked at 70). Rewired as an `appBus` `reveal-playing` event: browser.ts emits, each surface subscribes and self-guards (v1 requires genuinely-visible, v2 requires lab-active and glides via `homeIndex()`), keeping browser.ts surface-agnostic and graduation-proof. Verified on both surfaces (v2: position 32→89 = the played song's index; v1: committed card centered, y=535 vs 540). New instruments: `probe-timeline-swap.mjs` (rect dump + hairline pixel rows across the swap), `probe-summon-glide.mjs`, `probe-v2-glide.mjs`. Banked: when two DOMs serve one role, diff their rects across the swap before trusting either; a surface hidden by CSS is still alive and must guard its own event handlers.
- **2026-09-12 — The Summon redesigned: relevance-ranked sections, tinted songs, a whole frame.** The owner's brief: searching "Mili" should surface the artist, not twelve songs; songs rendered as oversized cards; and every row wore the same art. Root cause: the old matcher scored each entity against a combined text blob (title+artist+album), so an artist-field match equaled a name match, and the drawer printed fixed songs-first sections. Three shapes were built and lived with in one day (unified ranked list → fixed sections → relevance-ordered sections), the owner choosing after seeing each. Landed: (1) **one relevance scale** — a match on the entity's own name dominates (exact > prefix > word-coverage > substring > subsequence > typo), secondary fields count at 0.45×, and sections sort by their best hit with ties settling artist > song > album > playlist (owner ruling: a one-song album behind an album row is a wasted click — songs seat first on ties). (2) **Typo leniency** — a Damerau-Levenshtein rung below subsequence (budget 1 for short words, 2 for long, word-level, two-letter tokens exempt), strictly priced so leniency only extends results downward; the fuzzy matcher gained its first test suite (17 cases; `npm test` now 37). (3) **The drawer** — centered `── SECTION ──` headers, fixed Artists/Albums/Songs/Playlists vocabulary, per-section cap 5; songs are the only colored rows, flat rectangles washed with their album's prominence-weighted tone (index v7 `selectTone`, moonlight fallback, CDP-verified chroma over the void floor); albums/artists/playlists wear the pre-redesign full-bleed art strips at one unified row height, with the scrim re-weighted toward the text zone (94–80% void on the left, art breathing on the right) and subtext raised from `--dim` to 72% moon; the drawer's long-missing top border was restored so the frame reads whole, the top edge measuring ~2× the bottom with its bloom kept. The palette orb / sparkle row species and their tone-blending helpers (`deriveStarTone`/`blendRowTone`) were built, reviewed live, and removed the same day — owner verdict: sections plus symbol glyphs "need work" before they land; the old format rides until then. Clicking or pressing Enter on any result now dismisses the summon — its job is done (song rows were the gap; replaying the playing song still opens now-playing, after the close). Verified by `scripts/probe-summon.mjs` (19 checks through the app's CDP: relevance order, the hero tie, per-section caps, rendered pixel chroma for the song washes, subtext luminance, top-border presence, click-to-close, and playback via `document.title` — banked: the immortal audio element is `new Audio()` and never mounts in the DOM, so probes must verify playback by the title announcement). Banked laws: an honest ranking the eye cannot scan loses to an honest grouping — position truth only survives inside structures the eye can hold; ties must settle deterministically or sections flicker; and computed `color-mix` values return as `color(srgb ...)` notation — parse both spellings.
- **2026-09-12 — River v2 marked MOSTLY DONE; the star-identity exploration built, measured, reverted.** The universe color work ran the full loop one more time: a prominence-weighted star identity (per-album selectTone winners, albums as equal citizens) with a song lean (star tint = playing album's weighted tone, easing on the shortest hue path) landed and verified — RichaadEB's three albums drove the star 305° → 17.5° → 359°. Then per-album companion hues were staged end to end: offsets packed into the recipe uniform's free vec4 channel app-side, a ~4-line MKII hueRotate (sRGB-space YIQ rotation), the MKII suite grown to 24 checks — all green, motes measured at −7°/−22° in the live sky. And the owner looked at the actual universe and saw ZERO difference. At universe scale — an 11px star, 1–3px companions — a ±15° family band is below perception, and album-level granularity isn't central to the sky's vision in any case. Everything was reverted: the app commit reverted (`cb7cf6e`), the staging and the MKII experiment preserved in git stashes on both repos (MKII stash holds the hueRotate diff, the free-channel packing, and the 24-check suite incl. the differential companion assertions; the app stash holds a probe hardening — a differential edge-fade check immune to lyric-text pollution, plus a neutral-start re-assert). Banked law: a color fix must clear a VISIBILITY bar at production scale, not just a correctness bar — the pipeline was provably correct and pointless; measure the perceptual delta at production scale before building the machinery. River v2 stands at MOSTLY DONE: the F9 lab (contract surface, parametric depth layout, center lens, Shorten, edge dissolve, full input grammar, click-to-play + committed bloom, 40-check probe); riders are the reveal/collapse + spread-exit timeline, card recycling, the unmeasured fling fps bar, and graduation cleanup. The palette prominence pass (index v7) STANDS — ultraviolet renders purple, verified. The sky's star returns to the chroma-weighted tone over album palettes; palette weights stay stored (harmless, future-ready).
- **2026-09-13 — The song menu grows up: full-card panel, honest queueing, inline delete, and a real-mouse lesson.** The refinement pass on the 3D mount, driven by live use. (1) **Sizing:** the panel is now 280px wide and exactly as tall as its card — the owner killed the min-height floor with a cleaner design: a *readability guard* (cards rendering under ~55% of full card height decline the menu) — below that line cards are dissolving into the edge fade and their titles are unreadable, so no decision could be made there anyway; the jarring tiny-card-big-menu pairing cannot exist. The fork became two stacked frameless rows in the shared row grammar (left-aligned, hairline divider) instead of bordered buttons, so the whole panel speaks one language and the fork classes are already summon-row-shaped for the chrome mount; small phases (fork, confirm, word) sit vertically centered in the tall panel. (2) **The trigger stays on release** — the charter's press-trigger idea was retired by owner ruling (everything else fires on release; press-firing would feel weird); same-card right-click now toggles closed. (3) **A real bug the probes could never see:** real mouse clicks on the fork buttons never reached them — the card's pan logic captured the press and the click committed the song instead. Caught by probing with genuine CDP mouse input; `element.click()` bypasses pointer events entirely, so every synthetic-click probe was structurally blind to it. The panel now swallows its own pointerdowns. (4) **"Add to queue = play this next"** (owner ruling): in a library-sized queue the old refuse-always dedupe was a dead button — every song is always already queued. `queueNext` now moves a scheduled song to the front (`moved`), inserts an unscheduled one (`queued`), and no-ops at the front (`alreadyNext`); duplicates still never enter. (5) **Delete went inline** (owner ruling): Confirm|Cancel arm on the row itself (art and name stay), the row speaks "Deleted" and fades away while the rows below take its space, and the menu stays open on the shortened list — "Added" keeps the full-panel word because adding ends the menu's job. **Escape steps back one stage** (queue/list → fork, fork → close) while clicking off closes entirely. Probes grew to 28 checks with real-mouse input throughout; two instrument lessons banked: session-resume kv carries queue pollution across instances (normalize at start, assert deltas), and rects for scroll-clipped content point at nothing (scrollIntoView before measuring). Charter amended; chrome mount (summon rows + stage cells + transport button, `queuePanel.ts` retires) is the last pending item.
- **2026-09-13 — The chrome mount closes the song menu charter; the summon learns to share.** The one menu now serves every song surface. The flat chrome mount (`attachSongMenu`) landed on summon song rows, album stage cells, playlist detail rows, and the dormant flat list; the transport queue button opens the menu directly in queue phase, growing upward from the button (measured ruling: the button lives at the transport's left end, so it is left-aligned, not right-aligned as first proposed) and lights only while it owns the menu — the anchor row travels on new `song-menu-opened`/`song-menu-closed` appBus events. Toggle identity extends to rows (same-row right-click toggles closed), non-song summon rows decline (song-rows-only ruling), and the Escape ladder respects a track-less queue entry by closing directly. `queuePanel.ts` and `queue.css` are deleted — 798 lines of player chrome retired for a menu that already did everything. **The summon learned the layering for real:** live use caught the summon vanishing the moment Add to playlist was pressed — its press-dismissal saw the chrome menu's real presses (the menu lives at body level, outside the drawer) and closed the world behind the user's back. The synthetic-click probes were structurally blind to it, as they were to the capture hijack. The summon's dismissal now stands down while the menu is open: presses inside the menu never touch the summon, an outside press closes only the menu, the next press closes the summon. Probes stand at 41 checks with real-mouse add-flow coverage; all suites green.
- **2026-09-13 — River v2 graduates: the F9 lab is gone, the river is the Songs view.** The owner called it: remove the original river and the F9 toggle. Done in one pass. **Deleted:** `ui/songRiver.ts` (654 lines — the v1 river, its swipe grammar and mote confirmations dying with it per charter), `ui/riverV2Lab.ts`, `styles/river.css`, and every `river-v2-active` trace. **Born:** `ui/riverSurface.ts` — the production owner of the river contract, mounting at boot, visibility driven by the browser (`song-river-active` is now its dormancy flag; the flat list stays hidden behind it until Phase 3 parity), with the reduced `window.__riverV2Lab` probe handle kept deliberately as the verification ritual's eyes. The transport queue menu now centers on the queue icon, clamped to the viewport (the icon sits ~107px from the edge; center-when-it-fits keeps the anchor honest where physics allows it). **Probe lessons:** the river-surface probe now waits for the boot library rescan to settle before measuring (the surface mounts at boot, so old assumptions about lazy arming raced it), the edge-fade check samples a fresh region instead of one captured before the display-metrics round-trip (stale rectangles test the wrong pixels — it had been flaky across both eras for exactly this reason), the v1-era summon-glide probe retired (its v2 counterpart covers the now-only surface), the preview probe's dwell wait outraces its own 1400ms engage timer, and the summon wash check parks the virtual pointer before sampling — three probes' worth of hover-highlight and clip pollution from real-mouse residue. Full battery green and deterministic: surface 36/36 twice, menu 41, summon 19, preview, actions, glide, views, tab-swap. Remaining v2 riders: reveal/collapse + spread-exit timeline, card recycling, the fling fps bar.
- **2026-09-13 — The sky retires; Movement X rechartered as "The Library as Worlds."** The owner's verdict on the universe view: *"it feels like a very different art style from the rest of the application."* The diagnosis became a material law: every signed-off surface is a painting made of the library itself — album art, ink, hairlines — while the sky was the one surface with **no content in it**, generated light objects speaking astrophotography physics instead of the house's ink-and-void language. One hero star had read as art; a field of them read as a different artist's room. The direction was replaced through a long design session, and the replacement passes the content test by construction: **the shelf is where you choose, the river is what you see, the queue is what you hear.** The design: the shelf (home, two lenses) holds artist squares faced by their *newest* album's art, A–Z; entering an artist assembles their discography as a subtle-3D card river (newest first) via the river's card-push cascade; an album card falls into the **album place** — art filling the screen, blurred, its songs as the vertical river in one color family; **playlists are flat worlds one layer shorter** (square → song river, each row wearing its song's own album tone — the personal spectrum); the summon lands every result in its world; **Esc always walks exactly one stage home**. A front-door **selection layer** was folded in as law: boot focus = playing artist, else random (osu! landing); select → the horizon takes the artist's tone and the square whispers "N albums · M songs" → second click enters, clicking another square switches selection; the accent chain gains *selected > playing > moonlight* (deliberate selection, not hover — the hover law stands). **Phase 0 executed same day:** the universe surface, GL starfield, sky layout math, UNIVERSE tab, `universe-*` appBus events, `body.universe-active` veil grammar, the `sandbox/universe` + `sandbox/space` labs with their npm scripts, and fourteen sky-era probe/build scripts deleted; dead `probe:river-lab` npm entry swept. Full battery green: typecheck, 37/37, build, cold launch, nine probes (surface, menu, summon, preview, actions, glide, views, tab-swap, timeline-swap) all exit 0, stderr empty; the meridian starfield stays (bezel chrome, Movement IX). Charter: `docs/charter-movement-x-v2.md`; the old night-sky charter keeps a retirement banner and stays on disk as the rejected-direction record. Nothing is committed.
- **2026-09-14 — Phase 1: The Artists field — the shelf is live.** Built to charter §3.1 (`docs/charter-movement-x-v2.md`) on a new horizontal engine (`ui/shelf.ts`, porting the river v2 physics with its banked laws) plus `ui/shelfSurface.ts` and `styles/shelf.css`, mounted behind a temporary **SHELF** tab with the river untouched until parity. The field: six-to-eight faces visible, full-res newest-album art (lazy-loaded — 14 boot decodes were starving the river probe's timing checks), A–Z + # bucket, billboard recession with gentle tilt (≤14°), self-similar packing, alpha-mask edge dissolve, **infinite ring scroll** (wraps at ≥2× the visible count, small sets pin linear; the Shorten squash retires on a ring — a ring has no ends, and leaving it active compressed the seam), **snap-to-nearest at rest** via momentum-predicted targets (the fling becomes the settle — one motion, no overshoot-and-return; duration scales with travel), center lens +18% folded into the depth walk. **Center-focus grammar**: the field settles on one artist; the centered card auto-highlights — the horizon takes its tone, the ledger whisper ("N albums · M songs") shows on settle never mid-scroll; accent chain focused > playing > moonlight; side-click glides to center; center-click is the future enter (no-op); Esc never deselects. Boot focus: playing artist, else random. Lenses: ARTISTS / PLAYLISTS wordpair switcher (seated bottom-center — the instrument-strip relocation with the letter ruler is agreed and pending). Measured acceptance via `scripts/probe-shelf.mjs`: motion continuity (a card never moves further per frame than the scroll traveled, with the lens's 1.18× stretch accounted), edge pixel checks, determinism across reload. Build lessons banked: the **bloom stutter** — an animated box-shadow re-rasterizes its blur every frame and grows the layer texture mid-bloom (measured 45% frame collapse + two dropped frames); the bloom is now opacity on a pre-rastered layer (36/36 buckets at 144Hz, zero hitches) — and the first fix had clipped the glow inside the face's `overflow:hidden`, killing it silently, so the probe gained a bloom-visibility pixel check. The **text jitter** — art hides the device-pixel grid, crisp text exposes it — fixed with a three-layer split: face/bloom grid-snapped, caption on its own compositor layer with a fractional position in motion and a grid snap at rest (a first crisp-at-rest attempt via font-size reflow moved every caption and was reverted; the GPU-scale softness at rest is accepted for now). The **ledger color race** and **bloom color race** — reveal-time elements consuming the transitioning root `@property` enter in the previous tone; both receive baked literal colors at highlight time, and the probe asserts reveal-color === post-crossfade-color. The **ledger pixelation** — the ledger is only ever visible on the focused card, so it left the scaled caption for its own always-1:1 layer (zero falloff trade-off), position tracked per frame. A **transform-split hit-test regression**: untransformed wrappers stacked at screen center intercepted clicks and lied to rect queries — the tap surface is the transformed face. The CDP probe transport now uses Node's built-in WebSocket (the hand-rolled framer corrupted larger payloads, killing connections mid-probe). River probe hygiene: pointer parking, committed-state normalization, and transport-pulse zone exclusion in the edge-fade check (the pane glow pulses with the music into the sampled strip). Gates: typecheck, 37/37, build, cold launch, full battery 10/10. Nothing beyond §3.1 exists yet: places are Phases 2–4.
- **Deferred:** now-playing display toggles v1 — superseded by art/title cycling + persisted default view; richer toggles may return later.
- **Deferred:** custom folder-picker UI (frosted card style) — native dialog currently in use; queued for polish pass.
- **Open riders:** BPM tag shown in metadata when present · minor visual tweaks flagged during Movement VI sign-off review · custom cursor that morphs into the expanded-view edge chevrons.

## Future Direction

Movement VIII — **The Living Void** — is chartered and phased (see ledger): pointer bias (4), the usability block (5), song-state effects (6), and The Waveform Horizon (7) — a full-width, horizon-tinted waveform band above the timeline with lyrics riding over it and particles thrown by the music's own onsets. The Particle Lantern cursor shipped (2026-08-30): a canvas-drawn 48px pointer with flanking dust motes, click bursts, and a light ribbon, flag-gated and suppression-proof; per-state morphs await the owner's SVG art. The Songs view's playing row now carries the first owner-art asset — an auto-cropped, accent-tinted arrow pair shedding moment-driven motes via the new `moments.ts` onset primitive. Governing language: **solid means committed, dotted means preview**; music modulates through *moments*, never loudness; everything gated by the master and granular flags.

The north star beyond VIII: signature constellations growing into a navigable constellation map of the library — possibly a spatial, non-scroll alternative browsing view (prototyped as an option, never forced) — plus waveform-envelope figures, acoustic-similarity clustering, and the frosted folder-picker in Movement IX.

Beyond the charter: the frosted folder-picker and BPM-tag metadata stay open riders, waveform-envelope constellations may one day replace the hash seed, and acoustic-similarity clustering — constellations that drift toward albums that sound alike — remains the north star for a future movement.

## Known Behaviors

- First launch after an update runs a background rescan; palettes/cards refresh moments after boot (cached index paints instantly meanwhile).
- Sorting chip labels adapt per tab but occupy fixed 96px slots so the control bar never shifts.
- Artwork thumbnails (128px) are generated at scan and regenerated on demand: a missing thumbnail self-heals on its first request and then persists in the art cache.
